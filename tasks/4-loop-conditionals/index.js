//  Nie usuwaj tego
const liczby = [5, 32, 86, 2, 1]
const owoce = ['pomarancza', 'jablko', 2, 'banan', false]

// Pisz tutaj
for(i=0; i<7; i++) { 
    console.log(i)
}
function checkBooLean (checkMe) { 
    if (checkMe===true) { 
        return 'YEAH!';

    }
     else { 
        return 'Nie :<';
    }
    
}
console.log(checkBooLean(true));

for (let i = 0; i <= liczby.length; i++) { 
console.log (`Moj index to ${i}`)
console.log (`Moja wartość:` + liczby[i])

};
owoce.forEach(value)

    if (typeof value=== 'string'){
        console.log(value)
    }

    
